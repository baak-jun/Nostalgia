# 스킵 시 자동 태그(스킵) 부여

## 변경 파일
- lib/features/home/presentation/home.screen.dart

## 작업 내용
- 스와이프 스킵(위/아래) 처리 시 현재 사진 태그 세트에 `스킵` 태그를 자동 추가.
- 기존 태그가 있으면 유지하고, 중복 없이 set 기반으로 병합.

## 결정 사항
- 스킵 상태를 나중에 검색/필터링 가능하도록 자동 태그를 부여.

## 다음 작업
- TODO 참조
